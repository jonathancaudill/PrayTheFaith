# Optional: base URL of the API (omit to use same origin)
# VITE_API_URL=https://your-app.vercel.app

# --- Database (Supabase) ---
# Use the "Connection string" from Supabase: Project Settings → Database.
# Prefer "Connection pooling" (Transaction mode, port 6543) for serverless.
# POSTGRES_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# --- Local dev: fetch readings via Supabase REST (avoids hanging Postgres in Vite) ---
# From Supabase: Project Settings → API. Use "Project URL" and "anon public" key.
# Enable read access: Table Editor → readings → RLS: add policy "Allow public read" or disable RLS.
# VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# --- MCP (for list/approve/reject encouragements) ---
# Same value as in your MCP server / Cursor config.
# MCP_API_KEY=your-secret-for-mcp-endpoints
